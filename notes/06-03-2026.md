# 06-03-2026

## Homework from [06-02-2026](06-02-2026.md)

Carried forward (still open):

- [x] Follow up with George/Silvio on Crux access (EVITA project lacks an
      allocation; need either a project add or a different compute path).
- [ ] Ping Dr. Saumil Patel for the Taylor–Green Vortex CFD dataset (via
      Silvio's Slack intro).
- [x] Confirm DGX Spark user / access path with Victor.
- [ ] Workday + benefits onboarding with Melissa.
- [ ] Stand up `argo-shim` (or `argo-proxy`) on an HPC login node so OpenCode /
      SmolAgents can reach the Argo Gateway from compute.
- [x] Get the ParaView MCP server actually connecting
    - The current `pv_external_mcp` errors with
      `'vtkPythonStdStreamCaptureHelper' object has no attribute 'buffer'` and
      there is FastMCP API drift to resolve. - Solution to this was to create a
      Miniconda environment that specifically sets the values for Paraview and
      `mcp`. This is now reproducible via a Makefile directive called
      `create-dev`. Next step is to Dockerize this tool for further
      reproducibility.

New from
[06-02 team meeting (George / Silvio / Mengjiang / Jakub)](06-02-2026.md#Meeting%20With%20Silvio,%20Jakub%20+%20Menjiang):

- [ ] Walk through the NERSC MCP tutorial end-to-end and capture the bits that
      map onto our ParaView-MCP setup.
- [x] Port the ParaView MCP fixes from the SciVisAgent project upstream into
      `LLNL/paraview_mcp` (or a fork) so the server starts cleanly under current
      FastMCP.
- [ ] Stand up a basic ChatVis-in-SmolAgents prototype: re-implement a minimal
      slice of ChatVis with the prompt + context interfaces as explicit,
      swappable seams (this is the harness that Weeks 5–7 will sweep over).

## Week 2 Focus

Week 2 is "review + setup": land the MCP fixes, get one NL → ParaView round-trip
working end-to-end on a laptop, and stand up the ChatVis-mini harness so Week 5
can start benchmarking. The prompt-engineering vs. context-management framing
now drives the whole back half of the project.

The full revised 10-week plan now lives in
[`notes/artifacts/tasks.md`](tasks.md) — it supersedes the embedded table that
used to live here. Edit `tasks.md` (not this daily note) when the plan changes.

This week, in priority order:

1. ParaView MCP fix (port from SciVisAgent → `LLNL/paraview_mcp` fork). Blocks
   every downstream agent task.
2. NERSC MCP tutorial walk-through. Gives us a reference design for the agent ↔
   MCP loop and surfaces gaps in our current `pv_external_mcp` setup.
3. ChatVis-mini in SmolAgents. Minimum: one tool that invokes ParaView's Python
   API via the MCP server, one prompt template swappable from config, one
   context strategy (naive "dump the docs") in place as the baseline.
4. Crux access unblock (or pivot to Polaris/Sophia for development if Crux stays
   blocked through end of week).
5. Argo proxy on an HPC login node (sequence after #4 — depends on which host we
   land on).

### Notes

- Scope reminder from 06-02: the project is now explicitly framed as "compare
  prompt-engineering vs. context-management strategies for LLM-driven scientific
  visualization." The 06-02 scope-down excluded several research directions; see
  [`artifacts/tasks.md`](tasks.md) for the full list. If a question doesn't help
  us decide between prompt or context strategies, it's not Week 3–7 work.
- Target venue: AgenticAI4HPC (primary, best thesis fit); EduHPC as backup.
- Candidate LLM backends already confirmed: Claude Sonnet 4.6 + GPT 5.5 via Argo
  Gateway; Nemotron 3 Super 120B, Gemma4 31B, and Llama 3.3 70B via ALCF
  Inference. DGX Spark pending Victor.
- Compute: Polaris ✓, Sophia ✓, ALCF Inference ✓, ALCF Batch Inference ✓, Argo
  API ✓, Crux ✗ (blocked), DGX Spark ◐ (pending).

### Links

- Plan of record: [`artifacts/tasks.md`](tasks.md)
- Week 1 charter:
  [`artifacts/week1_deliverable.md`](artifacts/week1_deliverable.md)
- OpenCode ↔ Argo setup: [`artifacts/opencode.md`](artifacts/opencode.md)
- Prior day: [`06-02-2026.md`](06-02-2026.md)

## Meeting With Silvio, Menjiang, and Jakub

### Assigned Homework

- [x] Get ParaView-MCP connected to OpenCode and send a demo in the chat
- [ ] Find ParaView tutorials to learn how to use the GUI
- [ ] Look into SE workshops and conferences to submit this work to as a
      fallback

## Summary

ParaView MCP is finally running cleanly: the FastMCP API drift and the
`vtkPythonStdStreamCaptureHelper` error are resolved by pinning the environment
in a Miniconda recipe, captured as a `create-dev` Makefile target (Dockerization
is the next step). Crux access and the DGX Spark user are confirmed, leaving
Workday/benefits and the Saumil TGV dataset as the main organizational
carry-forwards. Re-prioritized Week 2 as a stack of five items in order: (1)
port the MCP fixes upstream into a fork of `LLNL/paraview_mcp`, (2) walk the
NERSC MCP tutorial, (3) stand up a ChatVis-mini in SmolAgents as the swappable
harness for Weeks 5–7, (4) unblock Crux (or pivot to Polaris/Sophia), (5) Argo
proxy on an HPC login node. The plan-of-record moved out of the daily note into
`artifacts/tasks.md`, AgenticAI4HPC is now the primary venue (EduHPC backup),
and the research framing tightened to "prompt-engineering vs. context-management
strategies for LLM-driven scientific visualization."

### How This Builds on [06-02-2026](06-02-2026.md)

Resolves the ParaView MCP blocker that has been open since 05-29 and that 06-02
reaffirmed as ASAP-critical. With the runtime working, the project can finally
execute on the 06-01/06-02 scope-down: today's Week 2 priority stack and the
ChatVis-mini harness are the concrete instantiation of yesterday's "ParaView MCP
PoC ASAP" direction. The single research question ("prompt vs. context") is a
sharper restatement of the MCP-vs-RAG framing from 06-01/06-02 and is what the
rest of the summer will be measured against.

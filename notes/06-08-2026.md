## Homework From [06-04-2026](06-04-2026.md)

- [ ] Workday + benefits onboarding with Melissa.
- [ ] Stand up `argo-shim` (or `argo-proxy`) on an HPC login node so OpenCode /
      SmolAgents can reach the Argo Gateway from compute.
- [ ] Walk through the NERSC MCP tutorial end-to-end and capture the bits that
      map onto our ParaView-MCP setup.
- [ ] Stand up a basic ChatVis-in-SmolAgents prototype: re-implement a minimal
      slice of ChatVis with the prompt + context interfaces as explicit,
      swappable seams (this is the harness that Weeks 5–7 will sweep over).

## Homework From [06-05-2026](06-05-2026.md)

- [x] Send GitHub repo URL for project(s) to Silvio on Monday
- [x] Log into Aurora to get access to the `/flare` filesystem
- [x] Get ParaView connected to `pvserver` while running MCP server
- [ ] Update [tasks](artifacts/tasks) with notes about completed goals
- [x] Get connected to ANL VPN to access Argo remotely
    - Only works on ANL devices

## eScience Homework

- [ ] Stage data on Zenodo if not already there
- [ ] Update FigShare + arXiv paper

## Tasks For Today

- [x] Test OpenCode default config alias
    - I reimplemented the default config with Argo support and connected only to
      the MCP server
- [x] `argo-shim` with OpenCode
    - Seems flakey. Currently have it working with the onsite version of Argo
- [x] Begin drafting introduction section of the paper

## Meeting With Silvio, Mengjiao, Victor, and Jakub

- Focus on getting ChatVis working with SmolAgents ASAP
- Store paper notes in a Zotero collection
- Keep revising the introduction

## Summary

Closed most of the 06-05 action items: project GitHub URLs sent to Silvio,
Aurora `/flare` access obtained, ANL VPN connected (only works on ANL-issued
devices), and ParaView is now connecting to `pvserver` with the MCP server
running. Rebuilt the OpenCode default-config alias to use Argo and to attach
only to the MCP server. `argo-shim` works against the onsite Argo through
OpenCode but is flaky. Began drafting the paper introduction. Meeting reaffirmed
the top priority: get ChatVis running under SmolAgents ASAP, and keep paper
notes in a Zotero collection.

### How This Builds on [06-05-2026](06-05-2026.md)

Closes the three concrete carry-forwards from 06-05 (GitHub URLs, Aurora
`/flare`, ParaView ↔ `pvserver` coupling while MCP runs) and adds the first
writing milestone (paper intro draft). The OpenCode + `argo-shim` work continues
the toolchain hardening that started 06-05 with the MCP CLI. The
ChatVis-in-SmolAgents goal, open since 06-03, is now explicitly the team's #1
priority going into the next day.

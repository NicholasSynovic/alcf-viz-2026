## Continued Implementation Of Skills

Focus on implementing the remaining coding snippets into the `paraview-coder`
skill. Then finalize the skill steps including the automated skill improvement

- [x] Integrate ParaView MCP code snippets into the `SKILL.md` files
- [x] Merge ParaView `SKILL.md` files
    - [x] [SciVisAgentSkills](https://github.com/KuangshiAi/SciVisAgentSkills/tree/main/paraview-viz)
    - [x] [HPC Skills](https://github.com/SciMate-AI/HPC-Skills/tree/main/skills/hpc-paraview)
        - Did not implement, between ChatVis, ParaView MCP, and
          SciVisAgentSkills all code snippets were already covered
    - [x] [ParaView Skills](https://github.com/TouKaienn/Paraview-Skills)

## Stripped Down ParaView MCP

Instead of having a MCP server that controls a ParaView instance, have an MCP
server that executes `pvpython` code on an instance

- [x] Move existing code into a `v1` directory
- [x] Migrate logging code into a common file
- [x] Migrate CLI code into a common file The original implementation of
      ParaView MCP **does not** support HTTP connections. A V2 version of this
      server will need to implement this first. Then we can iterate on a V3. The
      V3 will not be a stripped down version, but rather a new implementation
      focused on code execution over ParaView control.
- [x] Provide support for HTTP connections via the command line
- [x] Provide support for HTTP connections

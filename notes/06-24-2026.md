## Continuing to Strip ParaView MCP

- [x] Remove all existing tool calls
- [ ] Have a single tool call that accepts a Python script and returns
    - [ ] stdout
    - [ ] stderr
    - [ ] screenshot
    - Currently does not do any of these, focused on just a proof of concept of
      executing code on a remote

## ParaView connection on Crux

- [ ] Connect to Crux
- [ ] Find a tutorial on connecting to Crux from local ParaView instance

## Update

Was able to architect a new MCP connection where the MCP passes code to a
pvpython instance that executes on a pvserver instance. This allows for
arbitrary code execution
